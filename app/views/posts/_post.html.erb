<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>
  <%= image_tag(post.image, class: "attached_image") if post.image.present? %>
  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>
</div>